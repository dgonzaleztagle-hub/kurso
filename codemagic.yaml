workflows:
  ios-testflight:
    name: Kurso iOS TestFlight
    max_build_duration: 60
    integrations:
      app_store_connect: Codemagic
    environment:
      xcode: latest
      cocoapods: default
      ios_signing:
        distribution_type: app_store
        bundle_identifier: cl.mikurso.app
      vars:
        BUNDLE_ID: cl.mikurso.app
        APP_STORE_APPLE_ID: ""
        XCODE_WORKSPACE: ios/App/App.xcworkspace
        XCODE_SCHEME: App
    scripts:
      - name: Install dependencies
        script: npm ci
      - name: Build web assets
        script: npm run build
      - name: Sync Capacitor iOS shell
        script: npx cap sync ios
      - name: Set iOS build number
        script: |
          cd ios/App
          if [ -n "$APP_STORE_APPLE_ID" ]; then
            LATEST_BUILD_NUMBER=$(app-store-connect get-latest-testflight-build-number "$APP_STORE_APPLE_ID" || echo 0)
            agvtool new-version -all $(($LATEST_BUILD_NUMBER + 1))
          else
            agvtool new-version -all $BUILD_NUMBER
          fi
      - name: Install CocoaPods dependencies
        script: |
          cd ios/App
          pod install
      - name: Apply signing profiles
        script: xcode-project use-profiles
      - name: Build signed IPA
        script: |
          xcode-project build-ipa \
            --workspace "$XCODE_WORKSPACE" \
            --scheme "$XCODE_SCHEME"
    artifacts:
      - build/ios/ipa/*.ipa
      - /tmp/xcodebuild_logs/*.log
    publishing:
      app_store_connect:
        auth: integration
        submit_to_testflight: true
